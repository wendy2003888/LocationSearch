import { PipeUtilsPipe } from './pipe-utils.pipe';

describe('PipeUtilsPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeUtilsPipe();
    expect(pipe).toBeTruthy();
  });
});
